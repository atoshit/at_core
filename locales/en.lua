local EN <const> = {
    ['init.success'] = "The core has been initialized successfully, the metadata is now accessible in all the core.",
    ['init.failed'] = "The core has not been initialized correctly, all metadata is not necessarily accessible which can cause errors, checked your F8 console for errors and checked if you have configured everything in the `at_core_settings.cfg’."
}

return EN